# Required API keys and configurations
PK=<YOUR_PRIVATE_KEY> # Replace with your private key for deployment
AI_SERVICE_API_KEY=<YOUR_API_KEY>  # Replace with your AI provider's API key (we currently only support OpenAI)
RPC_ENDPOINT=https://api.mainnet-beta.solana.com # Replace with your RPC endpoint URL

# Trade parameters
TRADE_ACTION=buy          # Specify the action: "buy" or "sell"
TRADE_MINT=<YOUR_TOKEN_MINT_ADDRESS> # Insert the token contract address (mint address)
TRADE_AMOUNT=10000        # Specify the amount of SOL or tokens to be traded

# Debugging
NODE_DEBUG=module  # Enable debugging for module-level logging
